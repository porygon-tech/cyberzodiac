vectorBaby is a XOR learning neural network programmed using only numpy and pure python, since numpy is a faster choice than tf in simpler networks. 
It is a good way to learn how matrix multiplication is done in ML, as well as understanding backpropagation.
